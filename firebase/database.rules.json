{
  "rules": {
    ".read": false,
    ".write": false,

    "test": {
      ".read": "auth != null",
      ".write": "auth != null"
    },

    "device": {
      "led": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.isBoolean()"
      }
    }
  }
}
